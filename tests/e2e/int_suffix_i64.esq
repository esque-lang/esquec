// v0.11: explicit _i64 suffix on a literal returned as the function
// result. Exercises the 64-bit GPR path through CEIR/MIR and the
// REX.W-prefixed mov-imm32 / mov-imm64 forms in the encoder.
fn main() -> i64 = 42_i64
