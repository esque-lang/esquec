# v0.13: each can call any function whose effects fit the caller.
# Here `shout` is a user-defined @io wrapper around print_i32, and
# each(0..3, shout) produces 0\n1\n2\n on stdout.
@io
fn shout(x: i32) -> i32 = print_i32(x)

@io
fn main() -> i32 = { each(0..3, shout); 0 }
