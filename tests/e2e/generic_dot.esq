# expect exit 32
# Generic dot product function with shape polymorphism
fn dot[N: nat](a: f32[N], b: f32[N]) -> f32 = +/(a .* b)

fn main() -> i32 = {
    let x = [1.0, 2.0, 3.0];
    let y = [4.0, 5.0, 6.0];
    # dot(x, y) = 1*4 + 2*5 + 3*6 = 4 + 10 + 18 = 32
    dot(x, y) as i32
}
